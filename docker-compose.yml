services:
  backend:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      PORT: 3000

  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: http://localhost:3000/v0
    ports:
      - "8080:80"
    depends_on:
      - backend
